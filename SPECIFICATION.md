# VIVANT Voice 仕様書

## 1. 概要

VIVANTに登場するドラムが使うスマートフォンのように、入力した言葉を可愛い女性寄りの声で読み上げるWebアプリ。

作品内の音声や実在人物の声をそのまま再現するものではなく、ブラウザに搭載された日本語音声を使って、声の高さ・速度・間の取り方で特徴を表現する。

## 2. 現在の公開先

- GitHubリポジトリ: https://github.com/cosaca-taco/vivant-voice
- GitHub Pages: https://cosaca-taco.github.io/vivant-voice/
- 公開ファイル: `index.html`

GitHub Pagesで静的サイトとして公開している。

## 3. 現在の機能

### 3.1 音声読み上げ

- テキストエリアに入力した日本語を読み上げる
- ブラウザ標準の Web Speech API を利用する
- 日本語音声を優先して表示する
- 端末に女性寄りの日本語音声があれば優先選択する
- 音声が利用できない場合はエラーメッセージを表示する

### 3.2 話し方

話し方を次の3種類から選べる。

- ドラム風・特徴的
  - 音程を高めにする
  - 話す速度を少し遅くする
  - `！` や `。` の後に間を加える
- やさしく自然に
- 元気にはっきり

ブラウザやOSによって搭載音声が異なるため、実際の声質は利用環境に依存する。

### 3.3 定型句

以下の定型句をボタンで入力できる。

- 野崎さん、ありがとうございます！
- これで準備は完璧です！
- 薫先生、超かわいいですね！
- 任せてください！
- お疲れさまでした！
- 了解しました。

定型句ボタンは文章を入力欄へセットする。自動再生はせず、「話す」ボタンで再生する。

### 3.4 お気に入り

- 入力中の文章をお気に入りに保存できる
- 同じ文章の重複保存はしない
- 保存した文章を再入力できる
- 保存した文章を削除できる
- `localStorage` を使い、同じブラウザ・同じ端末に保存する
- サーバーやアカウントへの保存は行わない

### 3.5 UI

- スマートフォン向けのレスポンシブ画面
- 入力文字数を120文字までに制限
- 再生中・準備完了のステータス表示
- クリアボタン
- 声の種類選択
- 話し方選択

## 4. 技術構成

- HTML
- CSS
- JavaScript
- Web Speech API
- `localStorage`
- GitHub Pages

外部ライブラリは使用していない。

## 5. 音声仕様

音声合成は次のAPIを利用する。

```javascript
window.speechSynthesis
SpeechSynthesisUtterance
```

基本設定の考え方：

- 言語: `ja-JP`
- ドラム風の音程: 高め
- ドラム風の速度: やや遅め
- 句読点後: 必要に応じて間を追加

ブラウザ標準音声のため、Chrome、Safari、Edge、Android、iOSなどで声の種類と品質が変わる。

## 6. 試した構成と取りやめた構成

### VOICEVOX + Google Cloud Run

一度、VOICEVOX EngineをGoogle Cloud Runへデプロイし、GitHub PagesからAPIを呼び出す構成を試した。

構成は次のとおり。

```text
GitHub Pages
    ↓ HTTPS
Google Cloud Run
    ↓
VOICEVOX Engine
```

実施した内容：

- Google Cloudプロジェクト `vivant-voice` を作成
- 請求先アカウントを紐付け
- Cloud Run、Artifact Registry、Cloud Build APIを有効化
- VOICEVOX Engine CPU版をCloud Runへデプロイ
- CORSをGitHub PagesのOriginに限定
- 話者一覧の取得と音声生成を確認

しかし、VOICEVOXの声が希望するイメージと合わなかったため、この構成は取りやめた。

現在は以下を完了済み：

- Cloud Runサービス削除済み
- Google Cloudプロジェクト `vivant-voice` は削除申請済み
- 請求先リンク解除済み
- `gcloud` の既定プロジェクト設定解除済み

## 7. Google Cloudの状態

- プロジェクト名: `VIVANT-voice`
- プロジェクトID: `vivant-voice`
- 状態: `DELETE_REQUESTED`
- 請求先リンク: 解除済み
- Cloud Runサービス: 削除済み

Google Cloudのプロジェクト削除は即時完全消去ではなく、一定期間は復元可能な保留状態になる。

## 8. 使い方

1. GitHub Pagesを開く
2. 入力欄に読み上げたい文章を入力する
3. 必要に応じて「声の種類」を選ぶ
4. 「話し方」を選ぶ
5. 「話す」を押す
6. よく使う文章は「お気に入りに保存」を押す
7. 定型句は「よく使う言葉」から選ぶ

## 9. 変更時の公開手順

現在はGitHub PagesをGitHubリポジトリの `main` ブランチから公開している。

通常の変更手順：

1. ローカルの `index.html` を編集する
2. ブラウザで動作確認する
3. GitHubの `cosaca-taco/vivant-voice` に `index.html` をアップロードする
4. `main` ブランチへコミットする
5. GitHub Pagesのビルド完了を待つ
6. 公開URLで動作確認する

## 10. 今後の改善候補

- 端末ごとに音声の聞き比べボタンを追加する
- 声の高さをユーザーが調整できるスライダーを追加する
- 間の長さを調整できる設定を追加する
- 定型句をユーザー自身で追加できる機能を追加する
- お気に入りの並べ替え機能を追加する
- PWAとしてホーム画面に追加できる構成にする
- 音声ファイルを使う場合は、音声素材ごとの利用規約を確認する

## 11. 注意事項

- 特定作品の音声や実在人物の声を無断で再現・公開しない
- 音声モデルや音声素材を追加する場合は、商用利用、クレジット、再配布の条件を確認する
- Web Speech APIの対応状況と声質はブラウザ・OSごとに異なる
- お気に入りデータはブラウザのデータ削除や端末変更で失われる
- GitHub Pagesは静的ホスティングのため、サーバー側データ保存やサーバー音声合成は行わない
